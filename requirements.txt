# Core Framework
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6

# LLM Training
transformers==4.36.2
torch==2.1.1
accelerate==0.25.0
peft==0.7.1

# Optimization
# unsloth  # Optional: Install separately with: pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
bitsandbytes==0.41.3  # For quantization

# Axolotl (optional, for more advanced features)
# axolotl==0.4.1

# Data Processing
pandas==2.1.3
numpy==1.26.2
datasets==2.15.0

# Model Hub (for China users)
modelscope==1.11.0  # ModelScope support for faster model download in China

# Utilities
python-dotenv==1.0.0
pyyaml==6.0.1
tqdm==4.66.1
loguru==0.7.2

# Database/Cache
sqlalchemy==2.0.23

# Web Interface Dependencies (if running frontend from backend)
# These would be in frontend/package.json instead

# Development
pytest==7.4.3
black==23.12.0
ruff==0.1.8
